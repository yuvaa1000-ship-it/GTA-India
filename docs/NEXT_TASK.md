# Next task — after SECONDARY LIFE

Operation 06 is the stable milestone within its documented limited scope. Read AGENTS.md → OPERATION_LEDGER.md → SYSTEM_STATUS.md → relevant subsystem spec before editing. Operations 03–06 prompts are archived in docs/operations; supplied copies are reference. Continue the saved GitHub main milestone; do not recreate the project or repeat completed operations without a reproduced failure.

Next operation: **07 — MACHINE**, using its exact authorized prompt when supplied. Do not launch 07 during Operation 06. Production intent remains **60+ CU-class, RT-capable GPUs with 16 GB VRAM**; local Intel HD 2000 is compatibility-only. Current WebGL2 graphics are well below the final visual ambition and hardware RT is not implemented.

1. Recover with npm ci, npm test, npm run build, npm run preview -- --port 4173. Preserve the current player/physical/secondary architecture and all prior laboratories.
2. First integration boundary for MACHINE: replace or augment vehicle proxies with an explicitly owned vehicle chassis/wheel/tire simulation and rendering module, driven by actual mass and ground contacts. Resolve the exact vehicle, two-wheeler and rickshaw requirements from the 07 prompt before claiming their implementation. Connect vehicle collision events to OP05 mass-relative impulses without confusing injected events with real chassis contact.
3. Preserve OP05 controller-owned partial pelvis, final foot IK, all-dynamic falls, collider exclusion and clearance-checked recovery. Preserve OP06 ordering: restore facial offsets before base animation; apply face and hair/cloth attachments after physical pose. Vehicle/occupant work must not double-own these transforms.
4. Maintain bounded secondary ownership (maximum nine), exact resource release on LOD/streaming transitions, finite teleport/fall recovery and separate secondary CPU reporting. The current CPU implementation is expensive on the compatibility machine; optimize with measured evidence without silently removing hero quality. Target-GPU timing remains required and unavailable here.
5. Add the next concrete graphics increment: original vehicle silhouettes, wheels, lamps and material response with runtime inspection, measured submissions and documented limits. Complete character garments/grooms, anatomy, natural dialogue and advanced collisions remain outstanding production work; do not silently mark them done.
6. Run relevant automated and live secondary/reaction/motion/HUMAN/GENESIS/PHOTON checks when integration changes their ownership. Update ledger, status, next task and subsystem evidence; commit a stable milestone to the same GitHub repository before advancing.
