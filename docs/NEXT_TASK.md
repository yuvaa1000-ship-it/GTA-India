# Next task — after HUMAN

Remain on OPERATION 03 if the weighted character build or executable regressions fail. Read the canonical commit, SYSTEM_STATUS, HUMAN_SPEC, ANIMATION_SPEC, ARCHITECTURE and PHOTON specs before changing anything. Preserve GENESIS physics and PHOTON rendering. Do not regenerate citizen identities or restart the project.

1. Recover with npm ci, npm test, npm run build and the production preview. Run Validate humans, Run diagnostics and Compare rendering; record the actual backend, viewport and pass counts. Review geometry and contact counts after streaming.
2. Exact next character integration: replace the procedural swing driver behind HumanCharacter.pose with a contact-aware locomotion/retargeting controller under the next authorized operation. Feed actual displacement, grounded state and intended direction; preserve the 51 named joints or an explicit adapter. Measure foot sliding and ground penetration during walking, turning, stopping, jumping and stairs before advertising believable movement.
3. Integrate a license-verified original production human as a replaceable asset, not as identity storage. Validate metre scale, +Y up/+Z forward, feet pivot, weights, inverse bind matrices, morph topology, materials, clothing clearance, LODs and provenance. Retain the procedural fallback until the imported character runs and passes contact/deformation tests.
4. Improve facial anatomy, eyelids, mouth interior, wrinkles and skin response through authored topology and materials. Current primitive surfaces and three morphs are limited. Physical hair/cloth and facial speech remain separate systems.
5. For crowd continuity, retain cell/slot seeded identities while adding local avoidance, intent and reactions. Tier 1 capsules are broad contacts only; do not confuse them with AI or physical animation. Test collision lifetime and visual continuity when representations change.
6. Profile on an exact modern 60+ CU-class/RT-capable GPU, documented driver and resolution. Retain full hero fidelity and measure skinning, morphs, shadows, probe recapture and transmission separately. Current WebGL2 does not execute hardware RT. The local Intel GPU is only compatibility evidence.

The user intends 20 sequential operations with AAA visual ambition in an original Indian setting. Do not claim that operation count alone achieves GTA VI-level production quality. No later operation starts without its authorized prompt and a stable canonical preceding milestone.
