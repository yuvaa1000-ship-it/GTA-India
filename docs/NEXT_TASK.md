# Next task — after EUPHORIA INDIA

Operation 05 is the completed milestone within its documented scope. Read `AGENTS.md`, `OPERATION_LEDGER.md`, `SYSTEM_STATUS.md`, the relevant spec and the latest user instruction before editing. Operations 03/04 supplied again are reference; their exact prompts and the 05 prompt are preserved in `docs/operations/`. Do not restart them. Reproduce an actual failure before reopening completed work.

Next operation is **06 — SECONDARY LIFE**, using its exact authorized prompt when supplied. Do not launch it during Operation 05. Preserve modern high-end **60+ CU-class, RT-capable GPU** production intent and include its next concrete graphics increment. Local Intel HD 2000 is compatibility-only; this WebGL2 implementation does not provide hardware RT.

1. Recover the existing build with `npm ci`, `npm test`, `npm run build`, `npm run preview -- --port 4173`. GitHub source persists independently of the localhost process. Keep the existing game tab visible and retained at handoff.
2. The next exact technical integration is bounded secondary hair/garment motion driven by actual skeleton/root acceleration and environmental inputs, with stable ownership across ordinary motion, partial reactions, full falls, recovery and streamed disposal. Establish original strand/garment proxy data, rest shapes, collision boundaries, damping, timestep handling and LOD before claiming hair or cloth simulation. Compare rest, walk/run, impact/fall and recovery in the same camera/lighting.
3. Retain OP05 ownership rules: controller-owned partial pelvis, final visible-foot IK, all-dynamic full fall, collider exclusion, temporary wall-joint release and clearance-checked recovery. The 6/10 Hz pose-cadence tests verify actual ankle lift and planted contact. Do not remove these to hide regressions.
4. Remaining OP05 limitations are explicit follow-up work, not silently complete: dynamic get-up, naturally observed wall catches, anatomical joint cones/self-contact, physical crowd LOD, actual vehicle occupants and chassis impacts. Extend them when their owning operation requires it.
5. Improve character visual quality through the 06 hair/cloth silhouette and response, with original asset provenance and measured resource deltas. Record the increment in `GRAPHICS_PROGRESS.md`. A shader weave texture does not establish simulated cloth, and a screenshot does not establish motion.
6. Measure physics/secondary-motion/animation CPU separately from frame/render costs. Retain the complete hero quality independently of local FPS. High-end acceptance requires a real documented target GPU, driver, resolution and scene; it remains unmeasured.
7. Before handoff, update status, ledger, subsystem evidence and next exact task; run relevant live and automated regressions; commit one stable milestone to the same GitHub repository. Do not advance on an uncommitted or failing operation.
