# Next task

PHOTON has a recoverable rendering milestone with 26 code tests, 8 rendering checks and 11 gameplay checks passing. Remain on OPERATION 02 if recovery or regressions fail. Verify its canonical commit before advancing. GENESIS is the preserved foundation at `43114ceaaaf0eb5e82df5749fba5beb93f4c8dcd`. No later operation has been launched automatically.

1. Read `SYSTEM_STATUS.md`, `ARCHITECTURE.md`, `RENDERING_SPEC.md`, `PHOTON_TECHNIQUES.md`, `GEOMETRY_BUDGET.md`, and `PERFORMANCE_BUDGET.md` from canonical main. Run `npm ci`, `npm test`, `npm run build`, and `npm run preview`. Check the current commit and working tree before changing files.
2. Recover the production browser build and repeat both harnesses: **Run diagnostics** for the eleven gameplay/streaming checks and **Compare rendering** for eight rendering checks with four measured configurations. Review their actual pass/fail results and exported samples. Use `docs/evidence/photon-comparison.json` and `SYSTEM_STATUS.md` as the prior PHOTON evidence; historical GENESIS measurements belong to a different renderer/scene and are not a directly matched baseline.
3. **Next exact rendering integration step:** establish a repeatable baseline on a documented modern GPU while preserving the old-GPU findings. Fix viewport, device scale, camera pose, lighting, exposure policy, quality, adaptive-resolution state, and warm-up. Compare Balanced sunset with planar capture off/on first, then isolate scene shading, physical glass transmission, probe recapture/PMREM, and postprocessing costs one at a time. Record CPU submission, asynchronous GPU sample ages, mean/p95 wall-frame time, sample count, actual draw/triangle totals, resource counts, and capture frequency. Check for delayed metering/readback failures before interpreting exposure results.
4. Use a repeatable camera path to inspect the tradeoffs that fixed-camera profiles miss: grazing-angle wet asphalt and puddles, rapid orbit with throttled planar updates, glass and interior visibility, rough paint versus chrome, moving-object staleness in the probe, facade-window LOD changes, exposure recovery between bright signs and dark areas, and sunlight/shadow movement. Capture artifacts and their exact settings. Visual review and timing evidence must agree before selecting the next improvement.
5. Implement the best-supported fix from that profile with explicit resource ownership. Likely candidates are separating global sky IBL from the plaza probe, scheduling local probe recaptures, reducing the physical-transmission scene workload, and improving geometric edge stability. Do not add an expensive pass merely because it was named in the operation. Preserve the working WebGL2 reflection hierarchy; any WebGPU evaluation needs an actual adapter/device test and a separately run backend.
6. Rebuild, rerun both browser harnesses and relevant unit regressions, inspect the scene, update status/specs/evidence, and commit a stable milestone. Record remaining unsupported or experimental techniques honestly. Only a separately requested later operation should move into character production or broader game systems.

Current PHOTON scope is a working but limited rendering laboratory, not completion of every requested advanced technique. SSR/SSGI, cascades/PCSS, shadowed volumetrics, motion-vector temporal reconstruction, real skin/eye/hair scattering, and production HLOD/impostors remain future work. Only facade-window detail currently consumes the screen-space geometry policies; the listed hero/NPC/vehicle ceilings do not imply those assets exist.

Preserve the inherited checks for controller contact, pushing, cell ownership, save loading, and depot transitions. Continuing GENESIS limitations include worker timeout/retry, persistence beyond the 2048-record cap, cross-cell moved-prop correctness, camera smoothing near walls, hard world boundaries, and lack of actual traffic intelligence. Moving proxies can intersect the player, one another, and buildings at junctions. PHOTON has not replaced those systems with working AI or vehicle dynamics.
